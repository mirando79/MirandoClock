<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:background="#404040"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="12dp">

    <!-- Чекбокс для включения/выключения -->
    <CheckBox
        android:id="@+id/hourlyCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:buttonTint="#FFFFFF" />

    <!-- Текстовое поле: Час, Знак Зодиака и текст аффирмации -->
    <TextView
        android:id="@+id/hourlyLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_weight="1"
        android:text="1: Водолей (Текст аффирмации...)"
        android:textColor="#FFFFFF"
        android:textSize="16sp" />

    <!-- Кнопка для редактирования, которая запускает EditAffirmationActivity -->
    <Button
        android:id="@+id/hourlyAffirmationEditButton"
        style="@style/Widget.AppCompat.Button.Borderless"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:text="📝"
        android:textSize="24sp" />
</LinearLayout>
